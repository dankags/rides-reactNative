/* eslint-disable prettier/prettier */
import { Text, View } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const Rides = () => {
  return (
    <SafeAreaView>
    <View className='flex'><Text className=''>Rides</Text></View>
</SafeAreaView>
  )
}

export default Rides