/* eslint-disable prettier/prettier */
import { Text, View } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const Profile = () => {
  return (
    <SafeAreaView>
    <View className='flex'><Text className=''>Profile</Text></View>
</SafeAreaView>
  )
}

export default Profile